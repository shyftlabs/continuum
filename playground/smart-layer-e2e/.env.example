OPENAI_API_KEY=sk-...
# Optional: disable smart layer globally (RouterAgent falls back to llm routing)
# SMART_LAYER_ENABLED=true

# Optional: force every completion to one model (cheap local testing)
# TIER_FORCE_COMPLETION_MODEL=gpt-4o-mini
